name: Cyber Prod Deployment
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build deploy step
        run: echo "Build deploymnet started"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test step
        run: echo "Runinng test step"
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: deploy step
        run: echo "Running deploy step"
