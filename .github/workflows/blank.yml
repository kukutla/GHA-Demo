name: Cyber Prod Deployment
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "Build Step"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test step
        run: echo "Test Step"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy step
        run: echo "delpoy step"
