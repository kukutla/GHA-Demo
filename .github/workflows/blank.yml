name: Cyber App Deployment
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: buildstep
       run: echo "Cyber app build step"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: teststep
      run : "Cyber app test step"
   deploy:
     runs-on: ubuntu-latest
     steps:
       - name: deploystep
        run: "Cyber app deply step"
     
