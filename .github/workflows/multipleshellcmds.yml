name: Hello Workflow
on: workflow_dispatch
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello step
        run: |
          echo "This is first line"
          echo "This is second line"
